# Nested OpenMontage tree

`OpenMontage/` at the engine repo root is a duplicate checkout of tools, pipeline_defs, and skills. Do not import from it. The live tree is the repository root (`tools/`, `pipeline_defs/`, `skills/`, `runner/`).
